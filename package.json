{
  "name": "harderandwarner",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "astro check",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply harderandwarner --local",
    "db:migrate:remote": "wrangler d1 migrations apply harderandwarner --remote"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
